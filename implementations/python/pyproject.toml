[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "verbeat"
dynamic = ["version"]
description = "A 3D Versioning System for Real-World Dev Flow"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Avner Cohen", email = "israbirding@gmail.com"}
]
maintainers = [
    {name = "Avner Cohen", email = "israbirding@gmail.com"}
]
keywords = ["versioning", "git", "semantic", "calendar", "version management"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Version Control",
]
requires-python = ">=3.9"
dependencies = [
    "black>=25.1.0",
    "flake8>=7.3.0",
    "pytest>=7.0.0",
]

[project.urls]
Homepage = "https://github.com/AvnerCohen/verbeat"
Documentation = "https://github.com/AvnerCohen/verbeat#readme"
Repository = "https://github.com/AvnerCohen/verbeat"
"Bug Tracker" = "https://github.com/AvnerCohen/verbeat/issues"

[project.scripts]
verbeat = "verbeat:main"

[tool.setuptools.dynamic]
version = {attr = "verbeat._get_verbeat_version"}

[tool.setuptools.packages.find]
where = ["."]
include = ["verbeat*"]
exclude = ["tests*"]

[tool.setuptools.package-data]
verbeat = ["*.version"]

[project.optional-dependencies]
dev = [
    "build>=1.0.0",
    "twine>=4.0.0",
] 
